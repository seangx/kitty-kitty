# 合并 Pane 模式 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增全局 pane 模式：启用后每个 group 的会话作为 pane 共存于一个 tmux session，主 pane 占左 35%，其余右侧上下均分。

**Architecture:** 在 `cli-wrapper.ts` 加 `paneMode` 配置读取；`session-manager.ts` 新增 pane 布局管理函数；`session-handlers.ts` 新增 `session:create-in-group` IPC；`TagCloud.tsx` 组右键菜单增加「创建会话」；底栏脚本在 pane 模式下简化下层显示。模式切换时用 `join-pane` / `break-pane` 迁移。

**Tech Stack:** TypeScript, Electron IPC, tmux (join-pane/break-pane/swap-pane/split-window), SQLite, React

---

## File Structure

```
src/main/tmux/cli-wrapper.ts        # 修改: KittyConfig 加 paneMode, 导出 getPaneMode()
src/main/tmux/session-manager.ts     # 修改: 新增 pane 布局函数, 底栏 pane 模式分支
src/main/db/session-repo.ts          # 修改: groups 表新增 main_session_id 字段
src/main/db/database.ts              # 修改: migration 加 main_session_id
src/main/ipc/session-handlers.ts     # 修改: 新增 session:create-in-group IPC + pane 模式迁移
src/shared/types/ipc.ts              # 修改: 新增 IPC 常量
src/shared/types/session.ts          # 修改: GroupInfo 加 mainSessionId
src/renderer/pet/TagCloud.tsx        # 修改: 组右键菜单加「创建会话」
src/renderer/lib/ipc.ts             # 修改: 新增 IPC 调用方法
```

---

### Task 1: 配置读取 — getPaneMode

**Files:**
- Modify: `src/main/tmux/cli-wrapper.ts:64-81`

- [ ] **Step 1: 扩展 KittyConfig 接口和 loadConfig 默认值**

在 `cli-wrapper.ts` 中：

```typescript
interface KittyConfig {
  toolArgs?: Record<string, string>
  paneMode?: boolean
}

// 在 loadConfig() 的 defaultConfig 中加:
const defaultConfig: KittyConfig = { toolArgs: { claude: '' }, paneMode: false }
```

- [ ] **Step 2: 新增 getPaneMode 导出函数**

在 `getUserToolArgs` 之后添加：

```typescript
export function getPaneMode(): boolean {
  const config = loadConfig()
  return config.paneMode ?? false
}
```

- [ ] **Step 3: 验证 build**

```bash
cd ~/ai-workspace/kitty-kitty && npm run build
```

Expected: 编译通过。

- [ ] **Step 4: Commit**

```bash
git add src/main/tmux/cli-wrapper.ts
git commit -m "feat: add paneMode config support"
```

---

### Task 2: 数据库 — groups 表新增 main_session_id

**Files:**
- Modify: `src/main/db/database.ts:53-58`
- Modify: `src/main/db/session-repo.ts:26,117-170`
- Modify: `src/shared/types/session.ts:23-27`

- [ ] **Step 1: 在 database.ts 的 migration 中新增 main_session_id 列**

在 `database.ts` 的 `runMigrations` 函数中，在 `ALTER TABLE groups ADD COLUMN collab_enabled` 之后添加：

```typescript
    try {
      database.exec("ALTER TABLE groups ADD COLUMN main_session_id TEXT REFERENCES sessions(id) ON DELETE SET NULL")
    } catch { /* column already exists */ }
```

- [ ] **Step 2: 更新 session-repo.ts 的 GroupRow 和查询**

在 `GroupRow` 接口中添加字段：

```typescript
export interface GroupRow {
  id: string
  name: string
  color: string | null
  collabEnabled: number
  mainSessionId: string | null
}
```

更新 `listGroups()` 查询：

```typescript
export function listGroups(): GroupRow[] {
  const db = getDB()
  return db.prepare(`
    SELECT id, name, color, collab_enabled as collabEnabled,
           main_session_id as mainSessionId
    FROM groups
    ORDER BY created_at
  `).all() as GroupRow[]
}
```

更新 `getGroupById()` 查询：

```typescript
export function getGroupById(id: string): GroupRow | undefined {
  const db = getDB()
  return db.prepare(`
    SELECT id, name, color, collab_enabled as collabEnabled,
           main_session_id as mainSessionId
    FROM groups
    WHERE id = ?
  `).get(id) as GroupRow | undefined
}
```

新增 `setGroupMainSession()`：

```typescript
export function setGroupMainSession(groupId: string, sessionId: string | null): void {
  const db = getDB()
  db.prepare('UPDATE groups SET main_session_id = ? WHERE id = ?').run(sessionId, groupId)
}
```

- [ ] **Step 3: 更新 shared 类型**

在 `src/shared/types/session.ts` 的 `GroupInfo` 中添加：

```typescript
export interface GroupInfo {
  id: string
  name: string
  color?: string
  collabEnabled?: boolean
  mainSessionId?: string
}
```

- [ ] **Step 4: 验证 build**

```bash
cd ~/ai-workspace/kitty-kitty && npm run build
```

- [ ] **Step 5: Commit**

```bash
git add src/main/db/database.ts src/main/db/session-repo.ts src/shared/types/session.ts
git commit -m "feat: groups 表新增 main_session_id 字段"
```

---

### Task 3: tmux pane 布局管理函数

**Files:**
- Modify: `src/main/tmux/session-manager.ts`

在 `session-manager.ts` 中新增以下导出函数（放在 `killSession` 之后）：

- [ ] **Step 1: 新增 createPaneInSession — 在已有 tmux session 中 split 出新 pane**

```typescript
/**
 * Create a new pane in an existing tmux session by splitting.
 * First split: horizontal (right side, 65% width).
 * Subsequent splits: vertical within the right side.
 * Returns the new pane's tmux pane ID (e.g., %5).
 */
export function createPaneInSession(tmuxName: string, command: string, isFirstSplit: boolean): string {
  let paneId: string
  if (isFirstSplit) {
    // First additional pane: split horizontally, new pane gets 65% width
    paneId = execSync(
      `${TMUX} split-window -t ${shellQuote(tmuxName)} -h -p 65 -P -F '#{pane_id}' ${shellQuote(command)}`,
      { encoding: 'utf-8', env: { ...process.env, TERM: 'xterm-256color' } }
    ).trim()
  } else {
    // Subsequent panes: split the rightmost pane vertically
    // Find the last pane (rightmost/bottom)
    const panes = execSync(
      `${TMUX} list-panes -t ${shellQuote(tmuxName)} -F '#{pane_id}'`,
      { encoding: 'utf-8' }
    ).trim().split('\n')
    const lastPane = panes[panes.length - 1]
    paneId = execSync(
      `${TMUX} split-window -t ${lastPane} -v -P -F '#{pane_id}' ${shellQuote(command)}`,
      { encoding: 'utf-8', env: { ...process.env, TERM: 'xterm-256color' } }
    ).trim()
  }
  return paneId
}
```

- [ ] **Step 2: 新增 getPaneCount — 查询 tmux session 中 pane 数量**

```typescript
/**
 * Get the number of panes in a tmux session.
 */
export function getPaneCount(tmuxName: string): number {
  try {
    const output = execSync(
      `${TMUX} list-panes -t ${shellQuote(tmuxName)} -F '#{pane_id}'`,
      { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'ignore'] }
    ).trim()
    return output ? output.split('\n').length : 0
  } catch {
    return 0
  }
}
```

- [ ] **Step 3: 新增 swapMainPane — 将指定 pane 交换到主 pane 位置**

```typescript
/**
 * Swap a pane to the main (first/leftmost) position.
 * Used when user sets a new main pane via right-click menu.
 */
export function swapMainPane(tmuxName: string, targetPaneId: string): void {
  try {
    // Get the first pane ID
    const firstPane = execSync(
      `${TMUX} list-panes -t ${shellQuote(tmuxName)} -F '#{pane_id}' | head -1`,
      { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'ignore'] }
    ).trim()
    if (firstPane && firstPane !== targetPaneId) {
      execSync(`${TMUX} swap-pane -s ${targetPaneId} -t ${firstPane}`, { stdio: 'ignore' })
    }
  } catch { /* ignore */ }
}
```

- [ ] **Step 4: 新增 joinSessionAsPane — 将独立 session 合并为 pane**

```typescript
/**
 * Join an independent tmux session into a target session as a pane.
 * Used during session→pane mode migration.
 */
export function joinSessionAsPane(sourceSession: string, targetSession: string, isFirstJoin: boolean): void {
  const flag = isFirstJoin ? '-h -p 65' : '-v'
  // join-pane takes the source pane from another session
  execSync(
    `${TMUX} join-pane -s ${shellQuote(sourceSession + ':0.0')} -t ${shellQuote(targetSession)} ${flag}`,
    { stdio: 'ignore' }
  )
  // The source session is auto-destroyed when its last pane is moved
}
```

- [ ] **Step 5: 新增 breakPaneToSession — 将 pane 拆为独立 session**

```typescript
/**
 * Break a pane out of a session into its own independent session.
 * Used during pane→session mode migration.
 */
export function breakPaneToSession(paneId: string, newTmuxName: string): void {
  execSync(
    `${TMUX} break-pane -s ${paneId} -t ${shellQuote(newTmuxName)}`,
    { stdio: 'ignore' }
  )
}
```

- [ ] **Step 6: 验证 build**

```bash
cd ~/ai-workspace/kitty-kitty && npm run build
```

- [ ] **Step 7: Commit**

```bash
git add src/main/tmux/session-manager.ts
git commit -m "feat: pane layout management functions (create/swap/join/break)"
```

---

### Task 4: IPC — session:create-in-group + pane 模式切换

**Files:**
- Modify: `src/shared/types/ipc.ts`
- Modify: `src/main/ipc/session-handlers.ts`
- Modify: `src/renderer/lib/ipc.ts`

- [ ] **Step 1: 新增 IPC 常量**

在 `src/shared/types/ipc.ts` 中添加：

```typescript
  // Pane mode
  SESSION_CREATE_IN_GROUP: 'session:create-in-group',
  PANE_MODE_GET: 'pane-mode:get',
  PANE_MODE_SET: 'pane-mode:set',
  GROUP_SET_MAIN_SESSION: 'group:set-main-session',
```

- [ ] **Step 2: 在 session-handlers.ts 中注册新的 IPC handler**

在 `registerSessionHandlers()` 函数末尾（`collab:status` handler 之后）添加：

```typescript
  // --- Pane mode ---
  ipcMain.handle(IPC.PANE_MODE_GET, () => {
    const { getPaneMode } = require('../tmux/cli-wrapper')
    return getPaneMode()
  })

  ipcMain.handle(IPC.PANE_MODE_SET, (_event, enabled: boolean) => {
    const { loadConfig } = require('../tmux/cli-wrapper')
    const { writeFileSync, mkdirSync } = require('fs')
    const { join } = require('path')
    const { homedir } = require('os')
    const configPath = join(homedir(), '.kitty-kitty', 'config.json')
    
    // Read current config, update paneMode, write back
    let config: any = {}
    try {
      config = JSON.parse(require('fs').readFileSync(configPath, 'utf-8'))
    } catch { /* ignore */ }
    config.paneMode = enabled
    mkdirSync(join(homedir(), '.kitty-kitty'), { recursive: true })
    writeFileSync(configPath, JSON.stringify(config, null, 2))

    // Migrate tmux structure
    if (enabled) {
      migrateToPane()
    } else {
      migrateToSession()
    }

    tmux.refreshAllStatusBars()
    return { success: true }
  })

  ipcMain.handle(IPC.SESSION_CREATE_IN_GROUP, (_event, groupId: string) => {
    const { getPaneMode } = require('../tmux/cli-wrapper')
    ensureReady('claude')

    const group = sessionRepo.getGroupById(groupId)
    if (!group) throw new Error('Group not found')

    // Find the cwd from the group's existing sessions (use the main session's cwd)
    const groupSessions = sessionRepo.listSessionsByGroup(groupId)
    const mainSession = groupSessions.find(s => s.id === group.mainSessionId) || groupSessions[0]
    const cwd = mainSession?.cwd || undefined

    const script = generateLaunchScript('claude', 'new')

    if (getPaneMode() && groupSessions.length > 0 && mainSession) {
      // Pane mode: split into the group's tmux session
      const hostTmuxName = mainSession.tmuxName
      if (!tmux.isSessionAlive(hostTmuxName)) {
        throw new Error('Group host session is not running')
      }

      const isFirstSplit = tmux.getPaneCount(hostTmuxName) === 1
      const paneId = tmux.createPaneInSession(hostTmuxName, script, isFirstSplit)

      // Create DB record (session shares the tmux session name but has own ID)
      const { v4: uuid } = require('uuid')
      const id = uuid().slice(0, 8)
      const session: tmux.TmuxSession = {
        id,
        tmuxName: hostTmuxName, // same tmux session!
        title: `${group.name} agent`,
        tool: 'claude',
        cwd: cwd || '',
        status: 'running',
        createdAt: new Date().toISOString(),
      }
      sessionRepo.saveSession(session)
      sessionRepo.updateSessionGroup(id, groupId)

      // Set main session if not set
      if (!group.mainSessionId) {
        sessionRepo.setGroupMainSession(groupId, groupSessions[0]?.id || id)
      }

      try {
        sessionMcp.injectSessionMcp(id, cwd || '', hostTmuxName, session.title, groupId, group.name, 'claude', '', '')
        collab.watchInbox(id, hostTmuxName, 'claude')
      } catch (e) { log('session', 'mcp inject failed:', e) }

      tmux.focusSession(hostTmuxName)
      tmux.refreshAllStatusBars()
      return toSessionInfo(session)
    } else {
      // Session mode (or first session in group): create independent session
      const session = tmux.createTmuxSession('claude', undefined, cwd, script)
      sessionRepo.saveSession(session)
      sessionRepo.updateSessionGroup(session.id, groupId)

      // Set as main session if group has none
      if (!group.mainSessionId) {
        sessionRepo.setGroupMainSession(groupId, session.id)
      }

      try {
        sessionMcp.injectSessionMcp(session.id, session.cwd, session.tmuxName, session.title, groupId, group.name, 'claude', '', '')
        collab.watchInbox(session.id, session.tmuxName, 'claude')
      } catch (e) { log('session', 'mcp inject failed:', e) }

      tmux.attachSession(session.tmuxName)
      return toSessionInfo(session)
    }
  })

  ipcMain.handle(IPC.GROUP_SET_MAIN_SESSION, (_event, groupId: string, sessionId: string) => {
    sessionRepo.setGroupMainSession(groupId, sessionId)

    // In pane mode, swap the pane to main position
    const { getPaneMode } = require('../tmux/cli-wrapper')
    if (getPaneMode()) {
      const session = sessionRepo.listSessions().find(s => s.id === sessionId)
      if (session && tmux.isSessionAlive(session.tmuxName)) {
        // Find pane ID for this session — in pane mode sessions share tmuxName,
        // we need the actual pane_id. For now, this is best-effort.
        // The pane_id isn't stored in DB, so swap by the session's main_pane field.
        const mainPane = session.mainPane || '0.0'
        const target = mainPane.startsWith('%') ? mainPane : `${session.tmuxName}:${mainPane}`
        tmux.swapMainPane(session.tmuxName, target)
      }
    }
    return { success: true }
  })
```

- [ ] **Step 3: 新增迁移函数 migrateToPane 和 migrateToSession**

在 `session-handlers.ts` 中 `registerSessionHandlers` 函数外部添加：

```typescript
/**
 * Migrate from session mode to pane mode.
 * For each group with >1 session: join all sessions into one tmux session.
 */
function migrateToPane(): void {
  const groups = sessionRepo.listGroups()
  for (const group of groups) {
    const sessions = sessionRepo.listSessionsByGroup(group.id)
      .filter(s => tmux.isSessionAlive(s.tmuxName))
    if (sessions.length <= 1) continue

    // Pick main session (configured or first)
    const mainSession = sessions.find(s => s.id === group.mainSessionId) || sessions[0]
    const others = sessions.filter(s => s.id !== mainSession.id)

    // Join others into main session as panes
    for (let i = 0; i < others.length; i++) {
      try {
        tmux.joinSessionAsPane(others[i].tmuxName, mainSession.tmuxName, i === 0)
        // Update DB: the session now shares the main session's tmux name
        // (the old tmux session is auto-destroyed by join-pane)
      } catch (err) {
        log('pane-mode', `join failed for ${others[i].tmuxName}:`, err)
      }
    }

    // Set main session if not set
    if (!group.mainSessionId) {
      sessionRepo.setGroupMainSession(group.id, mainSession.id)
    }
  }
}

/**
 * Migrate from pane mode back to session mode.
 * For each group session with multiple panes: break panes into independent sessions.
 */
function migrateToSession(): void {
  const groups = sessionRepo.listGroups()
  for (const group of groups) {
    const sessions = sessionRepo.listSessionsByGroup(group.id)
    if (sessions.length <= 1) continue

    // Find the tmux session that hosts all panes
    const hostSession = sessions.find(s => tmux.isSessionAlive(s.tmuxName))
    if (!hostSession) continue

    const paneCount = tmux.getPaneCount(hostSession.tmuxName)
    if (paneCount <= 1) continue

    // Get all pane IDs
    try {
      const panes = execSync(
        `${tmux.TMUX} list-panes -t "${hostSession.tmuxName}" -F '#{pane_id}'`,
        { encoding: 'utf-8' }
      ).trim().split('\n')

      // Skip first pane (stays as main session), break the rest
      for (let i = 1; i < panes.length; i++) {
        const { v4: uuid } = require('uuid')
        const newTmuxName = `kitty_${uuid().slice(0, 8)}`
        try {
          tmux.breakPaneToSession(panes[i], newTmuxName)
          tmux.applyKittyStatusBar(newTmuxName)
        } catch (err) {
          log('pane-mode', `break failed for pane ${panes[i]}:`, err)
        }
      }
    } catch (err) {
      log('pane-mode', `migration failed for group ${group.id}:`, err)
    }
  }
}
```

- [ ] **Step 4: 在 renderer 的 ipc.ts 中添加调用方法**

查看 `src/renderer/lib/ipc.ts` 的现有模式，添加对应方法。如果它是直接用 `window.api.invoke()`，则无需修改（TagCloud 直接调用即可）。

- [ ] **Step 5: 验证 build**

```bash
cd ~/ai-workspace/kitty-kitty && npm run build
```

- [ ] **Step 6: Commit**

```bash
git add src/shared/types/ipc.ts src/main/ipc/session-handlers.ts src/renderer/lib/ipc.ts
git commit -m "feat: session:create-in-group IPC + pane mode migration"
```

---

### Task 5: 底栏 — pane 模式下简化下层

**Files:**
- Modify: `src/main/tmux/session-manager.ts` (applyKittyStatusBar, ensureSessionBarScript)

- [ ] **Step 1: 在 applyKittyStatusBar 中根据 paneMode 切换底栏行为**

修改 `applyKittyStatusBar`，在配置 status-format[1] 时检查 pane 模式：

```typescript
export function applyKittyStatusBar(tmuxName: string): void {
  try {
    const { getPaneMode } = require('./cli-wrapper')
    const groupBarScript = ensureGroupBarScript()
    const sq = shellQuote(tmuxName)

    // Initialize KITTY_ACTIVE_GROUP if not set
    try {
      const cur = execSync(`${TMUX} show-environment -g KITTY_ACTIVE_GROUP`, { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'ignore'] }).trim()
      if (!cur || cur.startsWith('-')) {
        execSync(`${TMUX} set-environment -g KITTY_ACTIVE_GROUP __ungrouped__`, { stdio: 'ignore' })
      }
    } catch {
      execSync(`${TMUX} set-environment -g KITTY_ACTIVE_GROUP __ungrouped__`, { stdio: 'ignore' })
    }

    const isPaneMode = getPaneMode()

    const opts: string[] = [
      `set-option -t ${sq} status on`,
      `set-option -t ${sq} status-position bottom`,
      `set-option -t ${sq} status-style "bg=#2a2a45,fg=#aaa8c3"`,
    ]

    if (isPaneMode) {
      // Pane mode: single line status (just group bar)
      opts.push(
        `set-option -t ${sq} status 1`,
        `set-option -t ${sq} status-format[0] "#[bg=#1e1e36]#(${groupBarScript})#[fill=#1e1e36,align=right]#[fg=#aaa8c3] %H:%M "`,
      )
    } else {
      // Session mode: dual line status
      const sessionBarScript = ensureSessionBarScript()
      opts.push(
        `set-option -t ${sq} status 2`,
        `set-option -t ${sq} status-format[0] "#[bg=#1e1e36]#(${groupBarScript})#[fill=#1e1e36]"`,
        `set-option -t ${sq} status-format[1] "#[bg=#2a2a45]#(${sessionBarScript})#[fill=#2a2a45,align=right]#[fg=#aaa8c3] %H:%M "`,
      )
    }

    opts.push(
      `set-window-option -t ${sq} window-status-format ""`,
      `set-window-option -t ${sq} window-status-current-format ""`,
      `set-option -t ${sq} status-interval 5`,
      `set-option -t ${sq} mouse on`,
    )

    for (const cmd of opts) {
      try { execSync(`${TMUX} ${cmd}`, { stdio: 'ignore' }) } catch { /* ignore */ }
    }

    // Keybindings
    const forkScript = ensureForkScript()
    const closeScript = ensureCloseScript()
    const binds = [
      'bind-key n switch-client -n',
      'bind-key p switch-client -p',
      `bind-key k choose-tree -sZ -F "#{session_name}"`,
      'bind-key -n M-Right split-window -h',
      'bind-key -n M-Down split-window -v',
      `bind-key -n M-Left run-shell -b "${closeScript} '#{pane_id}'"`,
      `bind-key -n M-f command-prompt -p "Fork branch:" "run-shell -b '${forkScript} \\\"%%\\\"'"`,
    ]
    for (const cmd of binds) {
      try { execSync(`${TMUX} ${cmd}`, { stdio: 'ignore' }) } catch { /* ignore */ }
    }

    bindGroupKeys()
    if (!isPaneMode) {
      bindSessionKeys()
    }
  } catch { /* ignore */ }
}
```

- [ ] **Step 2: 验证 build**

```bash
cd ~/ai-workspace/kitty-kitty && npm run build
```

- [ ] **Step 3: Commit**

```bash
git add src/main/tmux/session-manager.ts
git commit -m "feat: pane 模式下底栏简化为单行组栏"
```

---

### Task 6: UI — 组右键菜单增加「创建会话」

**Files:**
- Modify: `src/renderer/pet/TagCloud.tsx:722-739`

- [ ] **Step 1: 在组右键菜单中添加「创建会话」按钮**

在 `TagCloud.tsx` 的 `{/* Group context menu */}` 部分，在颜色选择器之前添加：

```tsx
      {groupCtxMenu && (
        <TagCtxMenu x={groupCtxMenu.x} y={groupCtxMenu.y} glass={theme.glass} dim={theme.dim}
          onClose={() => setGroupCtxMenu(null)}>
          {/* Create session in group */}
          <button onClick={async () => {
            try {
              await window.api.invoke('session:create-in-group', groupCtxMenu.id)
            } catch (e: any) {
              console.error('create-in-group failed:', e)
            }
            setGroupCtxMenu(null)
          }}
            style={{
              display: 'block', width: '100%', padding: '6px 12px', textAlign: 'left',
              background: 'none', border: 'none', color: '#e5e3ff', cursor: 'pointer',
              fontSize: 12, fontFamily: 'inherit', borderRadius: 6,
            }}
            onMouseEnter={(e) => { (e.target as HTMLElement).style.background = `${theme.dim}33` }}
            onMouseLeave={(e) => { (e.target as HTMLElement).style.background = 'none' }}
          >+ 在此组创建会话</button>
          {/* Color picker (existing) */}
          <div style={{ padding: '6px 10px 4px', display: 'flex', gap: 5, alignItems: 'center' }}>
            {/* ... existing color picker code ... */}
```

注意：保留现有的颜色选择器代码不变，只在它之前插入按钮。

- [ ] **Step 2: 在会话右键菜单中添加「设为主窗口」选项（仅 pane 模式下显示）**

在 `TagCloud.tsx` 的会话右键菜单项列表中，添加：

需要先从 renderer 获取 pane mode 状态。在组件顶部添加 state：

```tsx
const [paneMode, setPaneMode] = useState(false)
useEffect(() => {
  window.api.invoke('pane-mode:get').then(setPaneMode).catch(() => {})
}, [])
```

然后在右键菜单项数组中添加（条件显示）：

```tsx
...(paneMode && session.groupId ? [
  { label: '📌 设为主窗口', action: async () => {
    await window.api.invoke('group:set-main-session', session.groupId, session.id)
  }},
] : []),
```

- [ ] **Step 3: 验证 build**

```bash
cd ~/ai-workspace/kitty-kitty && npm run build
```

- [ ] **Step 4: Commit**

```bash
git add src/renderer/pet/TagCloud.tsx
git commit -m "feat: 组右键菜单增加「创建会话」+ 「设为主窗口」"
```

---

### Task 7: 功能验证

**Files:** 无代码变更

- [ ] **Step 1: 启动 dev 模式**

```bash
cd ~/ai-workspace/kitty-kitty
ps aux | grep 'kitty-kitty' | grep -v grep | awk '{print $2}' | xargs kill 2>/dev/null
npm run dev
```

- [ ] **Step 2: 验证默认行为（paneMode = false）**

1. 确认底栏仍为双层（组栏 + 会话栏）
2. 确认 Alt+数字切会话正常
3. 确认右键组有「在此组创建会话」选项
4. 点击「在此组创建会话」→ 应创建独立 tmux session 并分配到该组

- [ ] **Step 3: 开启 pane 模式**

编辑 `~/.kitty-kitty/config.json`，设置 `"paneMode": true`。

或通过右键/设置面板触发（如果已接入 UI）。

- [ ] **Step 4: 验证 pane 模式**

1. 确认底栏变为单行（仅组栏）
2. 在某个有多个会话的组上右键 → 「创建会话」
3. 确认新会话作为 pane 出现在右侧（主从布局）
4. 确认右键会话有「设为主窗口」选项，点击后 pane 交换到左侧

- [ ] **Step 5: 验证模式切换迁移**

1. 在 session 模式下创建两个会话并放入同一组
2. 切换到 pane 模式 → 确认两个会话合并为一个 tmux session 的两个 pane
3. 切回 session 模式 → 确认 pane 被拆回独立 session

- [ ] **Step 6: Commit 修复（如有）**

```bash
git add -A
git commit -m "fix: pane 模式验证修复"
```
